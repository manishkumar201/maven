name: Push to DockerHub 

on:
  push:

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: officialmanishkr98
          password: ${{ secrets.DOCKERHUB_PASS }}

